#include <stdio.h>

/**
 * main - program that prints all single digit numbers
 *
 * Description: numbers of base starting
 * program that prints all single numer
 * Return: 0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	printf("\n");
	return (0);
}

