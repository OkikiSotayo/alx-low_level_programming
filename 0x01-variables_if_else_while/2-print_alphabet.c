#include <stdio.h>

/**
 * main - another code test
 *
 * Description: function for putchar
 * this code prints putchar command
 * Return: 0
 */

int main(void)
{
	char ch;
	for (ch = 'a' ; ch <= 'z' ; ch++)

	{
		putchar(ch);
	}

	putchar('\n');
	return (0);
}
